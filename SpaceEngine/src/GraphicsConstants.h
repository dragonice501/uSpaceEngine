#pragma once

const int FPS = 60;
const int MILLISECONDS_PER_FRAME = 1000 / FPS;
const int PIXELS_PER_UNIT = 100;
const size_t SCREEN_SCALES = 10;

const float SCREEN_SCALE = 1.0f;

const float MOUSE_SENSITIVITY = 0.5f;